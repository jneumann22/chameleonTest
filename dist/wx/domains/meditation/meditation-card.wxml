<view class="screen-container ">
  <view class="card-container ">
    <view class="card-image ">
    <action-button text="Meditate Now" defaultButtonStyle="default-orange-button" defaultTextStyle="default-orange-button-text" handler="{{pressActionButton}}" />
     <Footer domain="domain" musePivot="muse_pivot" />
     <ImageIconButtons domain="{{domain}}" itemID="{{item_id}}" />
    </view>
  </view>
</view>